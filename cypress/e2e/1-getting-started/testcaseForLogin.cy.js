import {loginAll} from '../1-getting-started/subclass'

describe('Login Test', () => {
  it('should login with valid credentials', () => {
   loginAll("eva", "76578");
  });
});

describe('Login Test', () => {
  it('should login with valid credentials', () => {
   loginAll("sajee", "76ds578");
  });
});

describe('Login Test', () => {
  it('should login with valid credentials', () => {
   loginAll("johan", "76578");
  });
});